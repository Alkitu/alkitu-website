name: ✅ Task
description: Technical task or operational work
title: "[TASK] "
labels: ["task", "needs-triage"]
body:
  - type: markdown
    attributes:
      value: |
        ## Tarea Técnica
        Trabajo técnico u operacional (refactoring, updates, optimización, etc.)

  - type: textarea
    id: description
    attributes:
      label: 📝 Descripción
      description: Qué se necesita hacer y por qué
      placeholder: |
        **Qué:**
        Actualizar dependencias de React 19.2.0 a 19.2.3

        **Por qué:**
        Incluye fixes de seguridad críticos y mejoras de performance
    validations:
      required: true

  - type: textarea
    id: acceptance-criteria
    attributes:
      label: ✅ Criterios de Aceptación
      description: Cómo saber que la tarea está completa
      placeholder: |
        - [ ] Dependencias actualizadas en package.json
        - [ ] `npm install` ejecutado exitosamente
        - [ ] Build completa sin errores
        - [ ] Tests pasan (si aplica)
        - [ ] Funcionalidad verificada en dev
    validations:
      required: true

  - type: textarea
    id: approach
    attributes:
      label: 🔧 Enfoque Técnico
      description: Cómo se va a realizar la tarea
      placeholder: |
        **Pasos:**
        1. Actualizar package.json
        2. Ejecutar npm install
        3. Verificar breaking changes en changelog
        4. Ejecutar build y tests
        5. Probar en dev

        **Archivos a modificar:**
        - `package.json`
        - `package-lock.json`

        **Comandos:**
        ```bash
        npm update react react-dom
        npm run build
        npm test
        ```
    validations:
      required: false

  - type: textarea
    id: dependencies
    attributes:
      label: 🔗 Dependencias
      description: Otras tareas o tickets que deben completarse primero
      placeholder: |
        - Depende de #123 (Update TypeScript)
        - Bloquea #125 (Deploy to production)
    validations:
      required: false

  - type: dropdown
    id: priority
    attributes:
      label: 🎯 Prioridad
      options:
        - 🔴 Critical (P0)
        - 🟠 High (P1)
        - 🟡 Medium (P2)
        - 🟢 Low (P3)
    validations:
      required: true

  - type: dropdown
    id: type
    attributes:
      label: 🏷️ Tipo de Tarea
      options:
        - 🔄 Refactoring
        - 📦 Dependencies Update
        - ⚡ Performance Optimization
        - 🔒 Security Fix
        - 📚 Documentation
        - 🧹 Code Cleanup
        - 🔧 Configuration
        - 🧪 Testing
      multiple: false
    validations:
      required: true

  - type: textarea
    id: estimate
    attributes:
      label: ⏱️ Estimación
      description: Tiempo estimado y complejidad
      placeholder: |
        **Complejidad:** Baja / Media / Alta
        **Esfuerzo estimado:** 2 horas
    validations:
      required: false
